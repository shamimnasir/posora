---
import Base from '../../layouts/Base.astro';
import { bundle, digitalPacks } from '../../data/digital-packs';
import { bn } from '../../lib/bn';

export const prerender = false;
Astro.response.headers.set('cache-control', 'public, max-age=0, s-maxage=60, stale-while-revalidate=120');

const schema = [
  {
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'পসরা', item: 'https://posora.com/' },
      { '@type': 'ListItem', position: 2, name: 'ডিজিটাল প্যাক', item: 'https://posora.com/digital-pack/' },
    ],
  },
  {
    '@type': 'ItemList',
    name: bundle.title,
    numberOfItems: bundle.packs,
    itemListElement: digitalPacks.map((p, i) => ({
      '@type': 'ListItem', position: i + 1, name: p.title, description: p.hook,
      url: `https://posora.com/digital-pack/#${p.slug}`,
    })),
  },
];
const activitiesPerPack = digitalPacks[0]?.activities.length ?? 0;
const bonusPerPack = digitalPacks[0]?.bonus.length ?? 0;
const previewVersion = '20260919-varied';
---

<Base
  title="৳২৪৯ ডিজিটাল প্যাক বান্ডল"
  description="পসরার ১১ ভুবনের ঘর, স্কুল ও ছাপার জন্য তৈরি ডিজিটাল লার্নিং প্যাক। ৮৮টি ওয়ার্কশিট, ২২টি মিশন পাতা, ৩৩টি বোনাস উপকরণ, গাইড ও উত্তরপত্রের downloadable বান্ডল।"
  schema={schema}
>
  <div class="wrap page">
    <p class="crumbs"><a href="/">পসরা</a> <span>/</span> ডিজিটাল প্যাক</p>

    <section class="hero">
      <div class="hero-copy">
        <p class="eyebrow">ডাউনলোড বান্ডল · পরিবার ও শিক্ষক</p>
        <h1>এক বান্ডলে <em>শেখার পুরো ঘর</em></h1>
        <p class="lede">
          স্ক্রিনের ভেতরের পসরাকে টেবিলে, খাতায় আর ক্লাসে নিয়ে যাওয়ার জন্য ১১টি ভুবনের
          ছোট ছোট খেলার প্যাক। এক পাতায় এক কাজ, সঙ্গে কার্ড, গাইড আর উত্তরপত্র।
        </p>
        <div class="hero-actions">
          <a class="btn btn-primary" href="/digital-pack/checkout/">চেকআউট প্রস্তুতি দেখো →</a>
          <a class="btn preview-cta" href="#preview">ভেতরের নমুনা দেখুন ↓</a>
          <a class="btn" href="/family/#join">লঞ্চ তালিকায় নাম লেখাও</a>
          <a class="btn" href="/printables/">ফ্রি শিট আগে দেখো</a>
        </div>
        <p class="fine">একবারের মূল্য <b>৳{bn(bundle.priceBdt)}</b>। পেমেন্ট যাচাই হলে আপনার account-এ re-download access থাকবে।</p>
      </div>
      <div class="price-card" aria-label="বান্ডলের সারাংশ">
        <span class="price-kicker">১১ ভুবন · ১ বান্ডল</span>
        <strong>৳{bn(bundle.priceBdt)}</strong>
        <span>পেমেন্টের পর নিরাপদ member library access</span>
        <div class="stat-row"><b>{bn(bundle.activitySheets)}</b><span>টি ওয়ার্কশিট</span></div>
        <div class="stat-row"><b>{bn(bundle.packs)}</b><span>টি বিষয়ভিত্তিক প্যাক</span></div>
        <div class="stat-row"><b>{bn(bundle.bonusSets)}</b><span>টি কার্ড, গাইড ও উত্তরপত্র</span></div>
      </div>
    </section>

    <section class="value-strip" aria-label="বান্ডলে কী আছে">
      <div><span aria-hidden="true">✦</span><b>প্রিন্ট-রেডি</b><small>A4 · রঙিন ও সাদা-কালো</small></div>
      <div><span aria-hidden="true">↺</span><b>স্ক্রিন থেকে দূরে</b><small>টেবিলে বসে, একসঙ্গে</small></div>
      <div><span aria-hidden="true">✓</span><b>উত্তরপত্রসহ</b><small>অভিভাবক ও শিক্ষক দুজনের জন্য</small></div>
      <div><span aria-hidden="true">∞</span><b>একবার কিনে</b><small>নিজের ঘরে যতবার দরকার</small></div>
    </section>

    <section class="sec preview" id="preview">
      <div class="sec-h">
        <p class="eyebrow">কেনার আগে দেখে নিন</p>
        <h2>ভেতরের পাতার এক ঝলক</h2>
        <p>এগুলো আসল paid PDF থেকে নেওয়া নমুনা। ট্রেসিং, সাজানো, মাপা, নকশা, সিদ্ধান্ত আর পরীক্ষার পাতা যে সত্যিই আলাদা, তা কেনার আগেই দেখা যাবে।</p>
      </div>
      <div class="preview-tabs" role="tablist" aria-label="ডিজিটাল প্যাক preview">
        <button class="preview-tab is-active" type="button" role="tab" aria-selected="true" aria-controls="preview-sheets" id="tab-sheets">ওয়ার্কশিট</button>
        <button class="preview-tab" type="button" role="tab" aria-selected="false" aria-controls="preview-bonus" id="tab-bonus">বোনাস উপকরণ</button>
        <button class="preview-tab" type="button" role="tab" aria-selected="false" aria-controls="preview-guide" id="tab-guide">অভিভাবকের গাইড</button>
      </div>
      <div class="preview-panel is-active" id="preview-sheets" role="tabpanel" aria-labelledby="tab-sheets">
        <figure class="real-preview"><img src={`/digital-pack-preview/math-worksheet.png?v=${previewVersion}`} alt="গণিত খেলাঘর প্যাকের আসল ওয়ার্কশিটের নমুনা" loading="lazy" /><figcaption>আসল PDF-এর প্রথম ওয়ার্কশিট · সম্পূর্ণ প্যাকের একটি নমুনা পাতা</figcaption></figure>
        <div class="preview-copy"><span class="preview-index">০১</span><h3>শিশু শুধু টিক দেবে না</h3><p>প্রতিটি শিটে ছোট কাজ, আঁকার বা লেখার জায়গা এবং “আমি কী দেখলাম?” নিজের ভাবনার অংশ থাকে।</p><span class="preview-meta">A4 · রঙিন বা সাদা-কালো · ১৫ মিনিট</span></div>
      </div>
      <div class="preview-panel" id="preview-bonus" role="tabpanel" aria-labelledby="tab-bonus" hidden>
        <figure class="real-preview"><img src={`/digital-pack-preview/math-bonus.png?v=${previewVersion}`} alt="গণিত খেলাঘর প্যাকের বোনাস উপকরণের নমুনা" loading="lazy" /><figcaption>আসল বোনাস পাতার নমুনা · কাট-আউট, লক্ষ্য আর নিজের ভাবনার জায়গা</figcaption></figure>
        <div class="preview-copy"><span class="preview-index">০২</span><h3>শেখার পরে ঘরে থাকে</h3><p>লক্ষ্য কার্ড, মিশন ব্যাজ, রুটিন বোর্ড, হিসাব রাখার পাতা এবং উত্তর মিলানোর পাতা। এগুলো শিশুকে আবার বসতে সাহায্য করে।</p><span class="preview-meta">কাট-আউট · দেয়ালে লাগানোর কার্ড · পারিবারিক রুটিন</span></div>
      </div>
      <div class="preview-panel" id="preview-guide" role="tabpanel" aria-labelledby="tab-guide" hidden>
        <figure class="real-preview"><img src={`/digital-pack-preview/math-guide.png?v=${previewVersion}`} alt="গণিত খেলাঘর প্যাকের অভিভাবকের গাইডের নমুনা" loading="lazy" /><figcaption>আসল গাইড পাতার নমুনা · ঘরে ব্যবহারের ১৫ মিনিটের প্রশ্ন</figcaption></figure>
        <div class="preview-copy"><span class="preview-index">০৩</span><h3>শুধু ওয়ার্কশিট নয়</h3><p>প্রতিটি প্যাকে বয়স অনুযায়ী প্রশ্ন, ব্যবহার-পদ্ধতি, উত্তরপত্র এবং ঘরে কীভাবে কথাটি তুলবেন তার গাইড থাকে।</p><span class="preview-meta">পরিবার · শিক্ষক · ছোট দল</span></div>
      </div>
      <div class="preview-all-head"><h3>১১টি প্যাকের ১১টি আসল নমুনা পাতা</h3><span>প্রতিটি কার্ড খুলে নিজের প্যাকের প্রথম পাতাটি দেখুন</span></div>
      <div class="preview-grid">
        {digitalPacks.map((p) => (
          <a class="pack-preview-card" href={`/digital-pack-preview/${p.slug}.png?v=${previewVersion}`} target="_blank" rel="noreferrer" style={`--pack:${p.color}`}>
            <img src={`/digital-pack-preview/${p.slug}.png?v=${previewVersion}`} alt={`${p.title} আসল ওয়ার্কশিটের নমুনা`} loading="lazy" />
            <span class="pack-preview-info"><b>{p.title}</b><small>২টি মিশন পাতা · {p.activities.length}টি ওয়ার্কশিট · {p.bonus.length}টি বোনাস</small><em>বড় করে দেখুন ↗</em></span>
          </a>
        ))}
      </div>
    </section>

    <section class="sec pack-standard" id="every-pack">
      <div class="sec-h"><p class="eyebrow">প্রতিটি প্যাকে যা থাকছে</p><h2>শুধু পাতার bundle নয়, শেখার পুরো ধাপ</h2><p>প্রতিটি বিষয়ভিত্তিক প্যাকে একই নির্ভরযোগ্য ধাপ থাকবে, যাতে শিশু চেষ্টা, উত্তর বুঝে নেওয়া, নিজের ভাবনা বলা এবং আবার চেষ্টা করতে পারে।</p></div>
      <div class="standard-grid">
        <article><b>🎯 লক্ষ্য</b><p>এক লাইনের “আজ কী শিখব” এবং বয়স/সময় লেখা থাকবে।</p></article>
        <article><b>🧠 মনে করে বলা</b><p>আগে জানা কথা মনে করার ছোট প্রশ্ন থাকবে।</p></article>
        <article><b>✍️ করে দেখানো</b><p>কাটা, মেলানো, আঁকা, মাপা বা বানানোর hands-on task থাকবে।</p></article>
        <article><b>✓ উত্তর ও ব্যাখ্যা</b><p>উত্তর কী এবং কেন, দুটোই বোঝার জন্য উত্তরপত্র ও ইঙ্গিত থাকবে।</p></article>
        <article><b>↺ আবার চেষ্টা</b><p>ভুল হলে নতুন ইঙ্গিত নিয়ে আবার করার সুযোগ থাকবে।</p></article>
        <article><b>👨‍👩‍👧 ঘরে প্রয়োগ</b><p>অভিভাবক/শিক্ষকের ১৫ মিনিটের কথোপকথন ও বাস্তব জীবনের ছোট কাজ থাকবে।</p></article>
      </div>
    </section>

    <section class="sec research">
      <div class="sec-h">
        <p class="eyebrow">কেন এই জিনিসগুলো</p>
        <h2>যে কাজগুলো মানুষ সত্যিই খুঁজে নেয়</h2>
        <p>শুধু সুন্দর পাতা নয় - প্রতিটি প্যাক এমন কাজকে ছোট করেছে, যা বাড়ি বা ক্লাসে বারবার দরকার হয়।</p>
      </div>
      <div class="signal-grid">
        <article><b>অক্ষর লিখতে শেখা</b><p>ট্রেসিং, কার, যুক্তবর্ণ, ডিকটেশন ও ছবি-শব্দ - শুরুর বাংলা শেখার সবচেয়ে ঘন চাহিদা।</p></article>
        <article><b>গোনা ও হিসাব</b><p>সংখ্যা, যোগ-বিয়োগ, আকার, সময়, মাপ আর টাকা - একই পাতায় দেখা ও করার সুযোগ।</p></article>
        <article><b>সোর্ট, লেবেল, উত্তর</b><p>বিজ্ঞান শেখায় বাছাই, অংশের নাম, জীবনচক্র, খাদ্যশৃঙ্খল এবং আলাদা উত্তরপত্র।</p></article>
        <article><b>রুটিন ও অনুভূতি</b><p>ভিজ্যুয়াল রুটিন, প্রথমে-তারপর, অনুভূতি, বন্ধুত্ব ও শান্ত হওয়ার কার্ড - বাড়ির কাজ সহজ করে।</p></article>
      </div>
    </section>

    <section class="sec packs" id="packs">
      <div class="sec-h">
        <p class="eyebrow">ভেতরে যা পাবেন</p>
        <h2>১১টি প্যাক, ১১ রকমের “আজ কী করব?”</h2>
        <p>প্রতিটি প্যাকে ২টি মিশন পাতা, {bn(activitiesPerPack)}টি ছোট ওয়ার্কশিট, {bn(bonusPerPack)}টি সহায়ক বোনাস উপকরণ এবং বয়স অনুযায়ী ব্যবহার-পদ্ধতি।</p>
      </div>
      <div class="pack-grid">
        {digitalPacks.map((p, i) => (
          <article class="pack" id={p.slug} style={`--pack:${p.color}`}>
            <div class="pack-top"><span class="pack-no">{bn(i + 1)}</span><span class="world">{p.world}</span></div>
            <h3>{p.title}</h3>
            <p class="hook">{p.hook}</p>
            <p class="audience">{p.audience}</p>
            <ul>
              {p.activities.map((a) => <li>{a}</li>)}
            </ul>
            <div class="bonus"><b>বোনাস</b><span>{p.bonus.join(' · ')}</span></div>
            <p class="outcome"><b>ফল:</b> {p.outcome}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="sec bundle" id="bundle">
      <div>
        <p class="eyebrow">সবচেয়ে সহজ পছন্দ</p>
        <h2>আলাদা আলাদা নয় - সব ১১টি একসঙ্গে</h2>
        <p>আজ বাংলা লিখবে, কাল গাছ দেখবে, পরশু দোকান খেলবে। শিশুর আগ্রহ বদলালেও প্যাক বদলাতে হবে না।</p>
      </div>
      <div class="bundle-price"><span>একবারের মূল্য</span><b>৳{bn(bundle.priceBdt)}</b><small>পেমেন্ট যাচাই হলে member library-তে re-download</small><a class="btn btn-primary" href="/digital-pack/checkout/">নিরাপদ চেকআউট →</a></div>
    </section>

    <section class="sec honest">
      <h2>একটা সৎ কথা</h2>
      <p>এখনকার ৬টি ফ্রি শিট ফ্রিই থাকবে। ৳২৪৯ বান্ডল হবে তার ওপরের স্তর - আরও খেলা, কার্ড, উত্তরপত্র, অভিভাবক-গাইড আর ১১ ভুবনের ধারাবাহিক কাজ। পেমেন্ট গেটওয়ে চালু হলে লঞ্চ তালিকার মানুষ আগে জানবেন।</p>
      <a href="/family/#join">লঞ্চ তালিকায় নাম লেখাও →</a>
    </section>
  </div>
</Base>

<script>
  const previewTabs = [...document.querySelectorAll<HTMLButtonElement>('.preview-tab')];
  const previewPanels = [...document.querySelectorAll<HTMLElement>('.preview-panel')];
  previewTabs.forEach((tab) => tab.addEventListener('click', () => {
    previewTabs.forEach((t) => { t.classList.toggle('is-active', t === tab); t.setAttribute('aria-selected', String(t === tab)); });
    previewPanels.forEach((panel) => { const active = panel.id === tab.getAttribute('aria-controls'); panel.hidden = !active; panel.classList.toggle('is-active', active); });
  }));
</script>

<style>
  .page { padding-block: 1rem 4rem; display: flex; flex-direction: column; gap: 3rem; }
  .crumbs { font-size: 13px; color: var(--muted); }
  .crumbs span { margin: 0 .35rem; }
  .hero { display: grid; grid-template-columns: minmax(0, 1.45fr) minmax(260px, .7fr); gap: 2rem; align-items: center; }
  .hero-copy { display: flex; flex-direction: column; align-items: flex-start; gap: .85rem; }
  h1 { font-size: clamp(2.35rem, 6vw, 4.5rem); line-height: 1.08; max-width: 12ch; }
  h1 em { color: var(--brand); font-style: normal; }
  .lede { font-size: clamp(1.08rem, 2vw, 1.28rem); line-height: 1.72; color: var(--ink-2); max-width: 54ch; }
  .hero-actions { display: flex; flex-wrap: wrap; gap: .6rem; margin-top: .2rem; }
  .fine { color: var(--muted); font-size: 12px; line-height: 1.6; }
  .fine b { color: var(--brand-ink); }
  .price-card { background: linear-gradient(155deg, color-mix(in oklab, var(--brand) 26%, var(--panel)), var(--panel)); border: 1px solid color-mix(in oklab, var(--brand) 55%, var(--line)); border-radius: 26px; padding: 1.6rem; display: flex; flex-direction: column; gap: .7rem; box-shadow: 0 20px 50px rgba(0,0,0,.16); }
  .price-kicker { font-family: var(--font-mono); text-transform: uppercase; letter-spacing: .12em; font-size: 11px; color: var(--brand-ink); }
  .price-card > strong { font-family: var(--font-display); font-size: clamp(3.2rem, 8vw, 5rem); line-height: .95; color: var(--ink); }
  .price-card > span:not(.price-kicker) { color: var(--ink-2); font-size: 14px; }
  .stat-row { display: flex; align-items: baseline; gap: .5rem; border-top: 1px solid var(--line-soft); padding-top: .6rem; }
  .stat-row b { font-family: var(--font-display); color: var(--brand); font-size: 1.5rem; }
  .stat-row span { color: var(--ink-2); font-size: 13px; }
  .value-strip { display: grid; grid-template-columns: repeat(4, 1fr); gap: .7rem; }
  .value-strip > div { display: grid; grid-template-columns: 28px 1fr; column-gap: .55rem; align-items: center; padding: .8rem .9rem; border: 1px solid var(--line-soft); border-radius: var(--r-sm); background: var(--panel); }
  .value-strip span { grid-row: span 2; color: var(--brand); font-size: 1.4rem; }
  .value-strip b { font-size: 14px; }
  .value-strip small { color: var(--muted); font-size: 11px; }
  .preview { scroll-margin-top: 1rem; }
  .preview-tabs { display: flex; flex-wrap: wrap; gap: .55rem; border-bottom: 1px solid var(--line-soft); padding-bottom: .65rem; }
  .preview-tab { border: 1px solid var(--line); border-radius: 999px; padding: .65rem 1rem; background: var(--panel); color: var(--ink-2); font: inherit; cursor: pointer; }
  .preview-tab:hover, .preview-tab:focus-visible { border-color: var(--brand); color: var(--brand-ink); }
  .preview-tab.is-active { background: var(--brand); border-color: var(--brand); color: var(--on-brand); font-weight: 700; }
  .preview-panel { display: grid; grid-template-columns: minmax(0, 1.25fr) minmax(240px, .75fr); gap: 1.5rem; align-items: stretch; }
  .preview-panel[hidden] { display: none; }
  .sample-page, .bonus-preview, .guide-preview { min-height: 300px; border: 1px solid color-mix(in oklab, var(--brand) 35%, var(--line)); border-radius: 20px; padding: 1.35rem; background: linear-gradient(145deg, color-mix(in oklab, var(--brand) 16%, var(--panel)), var(--panel)); box-shadow: 0 18px 40px rgba(0,0,0,.1); }
  .sample-page { position: relative; overflow: hidden; }
  .sample-page::after { content: ''; position: absolute; width: 180px; height: 180px; border-radius: 50%; right: -65px; top: -70px; background: color-mix(in oklab, var(--brand) 28%, transparent); }
  .sample-top { display: flex; justify-content: space-between; color: var(--muted); font-size: 11px; font-family: var(--font-mono); }
  .sample-pill, .guide-label, .bonus-sticker { display: inline-block; margin-top: 1.5rem; padding: .4rem .7rem; border-radius: 999px; background: var(--brand); color: var(--on-brand); font-size: 11px; font-weight: 700; }
  .sample-page h3 { max-width: 18ch; margin: .75rem 0 .35rem; font-size: clamp(1.4rem, 3vw, 2rem); }
  .sample-page p, .guide-preview p { color: var(--ink-2); line-height: 1.65; max-width: 50ch; }
  .sample-dots { display: flex; gap: .55rem; margin: 1.35rem 0; }
  .sample-dots i { width: 34px; height: 34px; border-radius: 50%; background: color-mix(in oklab, var(--brand) 45%, var(--panel)); border: 2px solid var(--brand); }
  .sample-dots i:nth-child(2n) { transform: translateY(12px); background: color-mix(in oklab, #7cc9e8 55%, var(--panel)); }
  .sample-lines { display: grid; grid-template-columns: auto 1fr; gap: .55rem .8rem; align-items: end; color: var(--muted); font-size: 12px; }
  .sample-lines b { border-bottom: 1px dashed var(--muted); height: 1.25rem; }
  .sample-page small { display: block; margin-top: 1.4rem; color: var(--brand-ink); font-size: 11px; }
  .preview-copy { align-self: center; padding: .4rem .5rem; }
  .preview-index { color: var(--brand); font-family: var(--font-mono); font-size: 12px; }
  .preview-copy h3 { font-size: 1.7rem; margin: .35rem 0 .45rem; }
  .preview-copy p { color: var(--ink-2); line-height: 1.7; }
  .preview-meta { color: var(--brand-ink); font-size: 12px; }
  .real-preview { margin: 0; max-width: 520px; padding: .7rem; border: 1px solid var(--line); border-radius: 18px; background: var(--panel); box-shadow: 0 20px 45px rgba(0,0,0,.18); }
  .real-preview img { display: block; width: 100%; height: min(62vw, 430px); object-fit: contain; object-position: top center; border-radius: 11px; background: #eef3f6; }
  .real-preview figcaption { padding: .65rem .35rem .15rem; color: var(--muted); font-size: 11px; line-height: 1.5; }
  .preview-all-head { display: flex; align-items: baseline; justify-content: space-between; gap: 1rem; margin-top: .7rem; }
  .preview-all-head h3 { margin: 0; font-size: 1.35rem; }
  .preview-all-head span { color: var(--muted); font-size: 12px; }
  .preview-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: .8rem; }
  .pack-preview-card { display: flex; flex-direction: column; overflow: hidden; text-decoration: none; border: 1px solid color-mix(in oklab, var(--pack) 45%, var(--line)); border-top: 4px solid var(--pack); border-radius: 14px; background: var(--panel); transition: transform .2s ease, box-shadow .2s ease; }
  .pack-preview-card:hover, .pack-preview-card:focus-visible { transform: translateY(-4px); box-shadow: 0 14px 28px rgba(0,0,0,.18); }
  .pack-preview-card img { display: block; width: 100%; aspect-ratio: 210 / 297; object-fit: cover; object-position: top; background: #eef3f6; }
  .pack-preview-info { display: flex; flex-direction: column; gap: .25rem; padding: .65rem .7rem .75rem; }
  .pack-preview-info b { color: var(--ink); font-size: 13px; }
  .pack-preview-info small { color: var(--muted); font-size: 11px; }
  .pack-preview-info em { color: var(--brand-ink); font-size: 11px; font-style: normal; font-weight: 700; }
  .pack-standard { padding-top: .5rem; }
  .standard-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: .8rem; }
  .standard-grid article { padding: 1rem; border: 1px solid var(--line-soft); border-radius: 14px; background: var(--panel); }
  .standard-grid b { color: var(--brand-ink); }
  .standard-grid p { margin: .35rem 0 0; color: var(--ink-2); font-size: 13px; line-height: 1.6; }
  .bonus-preview { display: grid; grid-template-columns: repeat(3, 1fr); gap: .7rem; align-items: center; position: relative; }
  .bonus-sticker { position: absolute; top: 1rem; right: 1rem; margin: 0; transform: rotate(4deg); }
  .bonus-card { min-height: 150px; padding: .9rem; display: flex; flex-direction: column; gap: .55rem; justify-content: space-between; border-radius: 14px; background: color-mix(in oklab, var(--brand) 16%, var(--panel)); border: 1px dashed color-mix(in oklab, var(--brand) 55%, var(--line)); }
  .bonus-card b { color: var(--brand-ink); font-size: 13px; }
  .bonus-card span { color: var(--ink-2); font-size: 12px; line-height: 1.5; }
  .bonus-card i { height: 2rem; border-bottom: 1px solid var(--muted); }
  .guide-preview { background: linear-gradient(145deg, color-mix(in oklab, #75c9a6 18%, var(--panel)), var(--panel)); }
  .guide-preview h3 { font-size: 1.7rem; margin: 1.1rem 0 .25rem; }
  .guide-preview > div { display: flex; gap: .7rem; align-items: center; margin-top: .8rem; color: var(--ink-2); font-size: 13px; }
  .guide-preview > div span { flex: 0 0 27px; height: 27px; display: grid; place-items: center; border-radius: 50%; background: var(--brand); color: var(--on-brand); font-family: var(--font-mono); }
  .sec { display: flex; flex-direction: column; gap: 1.25rem; }
  .sec-h { max-width: 70ch; }
  .sec-h h2, .bundle h2, .honest h2 { font-size: clamp(1.7rem, 3.5vw, 2.5rem); line-height: 1.2; }
  .sec-h > p:last-child, .bundle p, .honest p { color: var(--ink-2); line-height: 1.7; }
  .signal-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: .8rem; }
  .signal-grid article { padding: 1.1rem; background: var(--panel); border: 1px solid var(--line-soft); border-radius: var(--r-sm); }
  .signal-grid b { color: var(--brand-ink); }
  .signal-grid p { margin: .35rem 0 0; color: var(--ink-2); font-size: 13px; line-height: 1.6; }
  .pack-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1rem; }
  .pack { padding: 1.2rem; border: 1px solid color-mix(in oklab, var(--pack) 38%, var(--line)); border-top: 5px solid var(--pack); border-radius: var(--r-sm); background: linear-gradient(145deg, color-mix(in oklab, var(--pack) 9%, var(--panel)), var(--panel)); }
  .pack-top { display: flex; justify-content: space-between; align-items: center; }
  .pack-no { width: 29px; height: 29px; border-radius: 50%; display: grid; place-items: center; color: var(--on-brand); background: var(--pack); font-family: var(--font-mono); font-size: 12px; }
  .world { color: var(--muted); font-size: 12px; }
  .pack h3 { font-size: 1.35rem; margin: .8rem 0 .15rem; }
  .hook { color: var(--brand-ink); font-weight: 700; }
  .audience { color: var(--muted); font-size: 12px; }
  .pack ul { margin: .8rem 0; padding-left: 1.1rem; columns: 2; }
  .pack li { color: var(--ink-2); font-size: 13px; line-height: 1.65; break-inside: avoid; }
  .bonus { display: flex; flex-direction: column; gap: .2rem; padding: .7rem .8rem; border-radius: 10px; background: color-mix(in oklab, var(--pack) 12%, var(--panel)); color: var(--ink-2); font-size: 12px; line-height: 1.55; }
  .bonus b { color: var(--pack); }
  .outcome { color: var(--ink-2); font-size: 13px; line-height: 1.6; margin-top: .7rem; }
  .outcome b { color: var(--ink); }
  .bundle { display: grid; grid-template-columns: 1fr auto; gap: 2rem; align-items: center; padding: 1.6rem; border: 1px solid color-mix(in oklab, var(--brand) 40%, var(--line)); border-radius: 20px; background: var(--brand-soft); }
  .bundle-price { display: flex; flex-direction: column; align-items: flex-end; gap: .25rem; min-width: 180px; }
  .bundle-price span, .bundle-price small { color: var(--brand-ink); font-size: 12px; }
  .bundle-price b { font-family: var(--font-display); font-size: 3rem; line-height: 1; color: var(--brand-ink); }
  .bundle-price .btn { margin-top: .7rem; }
  .honest { border-top: 1px solid var(--line-soft); padding-top: 1.4rem; max-width: 70ch; }
  .honest a { color: var(--brand-ink); font-weight: 700; }
  @media (max-width: 800px) { .hero { grid-template-columns: 1fr; } .value-strip, .signal-grid { grid-template-columns: repeat(2, 1fr); } .pack-grid { grid-template-columns: 1fr; } .bundle { grid-template-columns: 1fr; } .bundle-price { align-items: flex-start; } }
  @media (max-width: 900px) { .preview-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); } }
  @media (max-width: 640px) { .preview-panel { grid-template-columns: 1fr; } .preview-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); } .standard-grid { grid-template-columns: repeat(2, 1fr); } .bonus-preview { min-height: 260px; } .preview-all-head { align-items: flex-start; flex-direction: column; } }
  @media (max-width: 480px) { .value-strip { grid-template-columns: 1fr; } .pack ul { columns: 1; } h1 { max-width: none; } .preview-tabs { overflow-x: auto; flex-wrap: nowrap; padding-bottom: .8rem; } .preview-tab { white-space: nowrap; } .bonus-preview { grid-template-columns: 1fr; } .bonus-card { min-height: auto; } .preview-grid { gap: .55rem; } .pack-preview-info { padding: .5rem; } .pack-preview-info b { font-size: 11px; } .standard-grid { grid-template-columns: 1fr; } }
</style>
