# Luna: bounded scene implementation

Start only after the category brief, asset manifest and shared runtime interface are approved and available. If a dependency is missing, return blocked-dependency and its exact path; do not invent an interface. Implement ONE category in its owned module using the agreed reusable runtime. Keep educational content mappings intact and report unmapped items explicitly.

Use suitable authored geometry and material-specific PBR behavior: ceramic, glass, leaf, wood, metal, water and skin must not share a universal plastic finish. Do not apply legacy stylise() blindly to loaded PBR assets. Preserve scale and scientific meaning. Use an authored overview -> selected subject -> meaningful detail hierarchy, smooth bounded camera transitions, clear back/reset navigation, interrupted-transition handling, and bounds fitting at every stage/aspect ratio. Provide immediate reduced-motion transitions and a useful static fallback.

Use small unobtrusive hotspots and DOM labels that cannot cover the selected subject. Pause offscreen and hidden-tab animation. Handle late asset arrivals, category switching, context loss and disposal without orphan meshes or loops. Deterministic seeds enable repeatable screenshots. Do not change Toybox UI styling, payment features, unrelated categories, or shared runtime files.

Run relevant checks and provide real viewport screenshots plus a short interaction recording or time-sampled screenshots for all three shot levels. Record asset bytes, renderer.info draw calls/triangles, device/browser and timing method. A desktop headless test is not proof of real-phone GPU performance. Two unsuccessful iterations go to Sol or Astra with evidence, not a third blind attempt.
